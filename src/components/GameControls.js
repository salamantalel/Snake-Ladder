import React from "react";

function GameControls() {

    return (
        <div>
            <h2>GameControls</h2>
            <p>this is where start roll or reset buttons</p>
        </div>
    );
}

export default GameControls;